案例需求：
    1.  访问带有验证码的登录页面login.jsp
    2.  用户输入用户名，密码以及验证码。
        ·如果用户名和密码输入有误，跳转登录界面，提示：用户名和密码错误‘
        ·如果验证码输入有误，跳转登录页面，提示：验证码有误。
        ·如果全部输入正确，则跳转到主页面success.jsp，显示：用户名，欢迎您。

分析：
  1.  设置request的编码。
  2.  获取参数Map集合。
  3.  获取验证码。
  4.  将用户信息封装到User对象。
  5.  判断程序生成的验证码和用户输入的验证码是否一致。